module safety

struct Ticket {
    n: int
}

fn take_ticket(t: Ticket) -> int {
    t.n
}

fn bump_int(ref mut value: int) {
    value[] = value[] + 1
}

pub fn prove_safety() {
    a = Ticket { n: 7 }
    v = take_ticket(a)
    println(v)
    mut x = 40
    bump_int(ref mut x)
    println(x)
}
