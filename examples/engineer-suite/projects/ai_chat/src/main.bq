# Needs a running serve: buraaq ai serve MODEL
# Then: buraaq run
module main

use std.ai
use std.io.{println}

fn main() {
    m = ai.model("Qwen/Qwen3-8B")
    ai.system(m, "You are a concise Buraaq assistant.")
    println(ai.chat(m, "Say hello in one short sentence."))
}
