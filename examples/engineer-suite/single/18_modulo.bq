# Modulo / integer division (common in engineering loops)
use std.io.println

fn main() {
    mut sum = 0
    mut i = 0
    while i < 10 {
        i = i + 1
        if i % 3 == 0 {
            sum = sum + i
        }
    }
    println(sum.to_text())
}
