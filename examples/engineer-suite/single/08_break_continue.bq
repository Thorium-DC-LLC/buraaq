# break / continue in loops
use std.io.println

fn main() {
    mut sum = 0
    mut i = 0
    while i < 20 {
        i = i + 1
        if i == 13 {
            break
        }
        if i % 2 == 0 {
            continue
        }
        sum = sum + i
    }
    println(sum.to_text())
}
