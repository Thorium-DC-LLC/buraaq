# Experimental probes

These programs document **syntax we want** but do not yet require to pass `buraaq run`.
The suite runner runs `buraaq check` on them and reports pass/fail without failing the whole suite.

Use them to find the next compiler/stdlib gaps (async, channels, parallel for, collections, etc.).
