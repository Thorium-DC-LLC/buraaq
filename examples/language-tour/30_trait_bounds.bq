# 30 — trait bounds
trait Comparable {
    fn lt(self, other: Self) -> bool
}

fn min[T: Comparable](a: T, b: T) -> T {
    if a.lt(b) { a } else { b }
}

fn main() {
    print(min(3, 7).to_text())
}
