[package]
name = "buraaq_ownership"
description = "Definite initialization and move semantics for Buraaq"
version.workspace = true
edition.workspace = true

[dependencies]
buraaq_diagnostics = { path = "../diagnostics" }
buraaq_source = { path = "../source" }
buraaq_types = { path = "../types" }
