# Zed

Install as a [Zed extension](https://zed.dev/docs/extensions) from this folder, or copy `extension.toml` settings into your Zed `settings.json` language overrides and point LSP at `buraaq lsp-server`.

Until a tree-sitter grammar ships, Zed still attaches LSP for diagnostics and completion.
